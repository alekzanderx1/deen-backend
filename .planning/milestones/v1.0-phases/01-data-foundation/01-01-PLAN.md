---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - core/config.py
  - .gitignore
autonomous: true
requirements:
  - INGE-04
  - INGE-05
  - INGE-06

must_haves:
  truths:
    - "requirements.txt contains pymupdf==1.27.2.2 and pinecone-text==0.11.0"
    - "core/config.py exports DEEN_FIQH_DENSE_INDEX_NAME and DEEN_FIQH_SPARSE_INDEX_NAME via os.getenv()"
    - ".gitignore contains data/*.json to exclude the persisted BM25 encoder from version control"
    - "pip install -r requirements.txt completes without errors (both new packages install cleanly)"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned pymupdf and pinecone-text dependencies"
      contains: "pymupdf==1.27.2.2"
    - path: "core/config.py"
      provides: "Fiqh index env var exports"
      exports: ["DEEN_FIQH_DENSE_INDEX_NAME", "DEEN_FIQH_SPARSE_INDEX_NAME"]
  key_links:
    - from: "core/config.py"
      to: "scripts/ingest_fiqh.py"
      via: "from core.config import DEEN_FIQH_DENSE_INDEX_NAME, DEEN_FIQH_SPARSE_INDEX_NAME"
      pattern: "DEEN_FIQH_DENSE_INDEX_NAME\\s*=\\s*os\\.getenv"
---

<objective>
Add two missing Python dependencies (pymupdf, pinecone-text) to requirements.txt, register two new Pinecone index env vars in core/config.py, and add a .gitignore entry for the BM25 encoder JSON artifact.

Purpose: These are the prerequisite infrastructure changes that the ingestion script (plans 02 and 03) depends on. No ingestion code can be written or run until these exist.
Output: Updated requirements.txt, updated core/config.py with two new exported constants, updated .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing core/config.py pattern the executor must replicate exactly. -->

From core/config.py (current state):
```python
import os
from dotenv import load_dotenv

load_dotenv()

# Existing index names — add fiqh vars AFTER these three lines:
DEEN_DENSE_INDEX_NAME = os.getenv("DEEN_DENSE_INDEX_NAME")
DEEN_SPARSE_INDEX_NAME = os.getenv("DEEN_SPARSE_INDEX_NAME")
QURAN_DENSE_INDEX_NAME = os.getenv("QURAN_DENSE_INDEX_NAME")

# ... rest of file ...

# Existing validation guard (DO NOT add a guard for fiqh vars here):
if not DEEN_DENSE_INDEX_NAME or not DEEN_SPARSE_INDEX_NAME:
    raise ValueError("Missing Index names! Ensure they are set in the .env file.")
```

CRITICAL: Do NOT add a ValueError guard for DEEN_FIQH_DENSE_INDEX_NAME or DEEN_FIQH_SPARSE_INDEX_NAME
at module import time. The existing guard on DEEN_DENSE_INDEX_NAME already blocks server startup
if those vars are absent. A guard on fiqh vars would prevent the server from starting for developers
who haven't set up fiqh indexes yet. Guard inside the ingestion script only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pymupdf and pinecone-text to requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt — read current contents to find alphabetically correct insertion point and verify neither package is already present
  </read_first>
  <action>
Add the following two lines to requirements.txt in their correct alphabetical positions (the file is alphabetically ordered):

  - `pymupdf==1.27.2.2` — insert between the `py*` entries (near `pyzmq` or similar)
  - `pinecone-text==0.11.0` — insert in the `p*` section near other `pinecone*` entries

Both versions are locked to the exact values confirmed by research on 2026-03-23. Do not use version ranges or >= constraints.

After inserting both lines, verify the file still reads cleanly (no duplicates, no syntax errors).
  </action>
  <verify>
    <automated>grep -n "pymupdf==1.27.2.2\|pinecone-text==0.11.0" /Users/shawn.n/Desktop/Deen/deen-backend/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exactly the line `pymupdf==1.27.2.2`
    - requirements.txt contains exactly the line `pinecone-text==0.11.0`
    - Neither package appears more than once in the file
  </acceptance_criteria>
  <done>Both packages are pinned in requirements.txt at the verified versions.</done>
</task>

<task type="auto">
  <name>Task 2: Register fiqh index env vars in core/config.py</name>
  <files>core/config.py</files>
  <read_first>
    - core/config.py — read the full file to see the existing DEEN_DENSE_INDEX_NAME / DEEN_SPARSE_INDEX_NAME / QURAN_DENSE_INDEX_NAME block; insert new lines immediately after that group
  </read_first>
  <action>
In core/config.py, add the following two lines immediately after the `QURAN_DENSE_INDEX_NAME = os.getenv("QURAN_DENSE_INDEX_NAME")` line (which is line 12 in the current file):

```python
DEEN_FIQH_DENSE_INDEX_NAME = os.getenv("DEEN_FIQH_DENSE_INDEX_NAME")
DEEN_FIQH_SPARSE_INDEX_NAME = os.getenv("DEEN_FIQH_SPARSE_INDEX_NAME")
```

Do NOT add any validation guard (if not DEEN_FIQH_DENSE_INDEX_NAME: raise ValueError(...)) at module level. The ingestion script will guard these values itself. Adding a module-level guard would break server startup for developers who haven't configured fiqh indexes.

No other changes to core/config.py.
  </action>
  <verify>
    <automated>grep -n "DEEN_FIQH_DENSE_INDEX_NAME\|DEEN_FIQH_SPARSE_INDEX_NAME" /Users/shawn.n/Desktop/Deen/deen-backend/core/config.py</automated>
  </verify>
  <acceptance_criteria>
    - core/config.py contains `DEEN_FIQH_DENSE_INDEX_NAME = os.getenv("DEEN_FIQH_DENSE_INDEX_NAME")`
    - core/config.py contains `DEEN_FIQH_SPARSE_INDEX_NAME = os.getenv("DEEN_FIQH_SPARSE_INDEX_NAME")`
    - core/config.py does NOT contain `raise ValueError` referencing `DEEN_FIQH_DENSE_INDEX_NAME`
    - The two new lines appear adjacent to the existing DEEN_DENSE_INDEX_NAME / QURAN_DENSE_INDEX_NAME block (not scattered elsewhere in the file)
  </acceptance_criteria>
  <done>core/config.py exports DEEN_FIQH_DENSE_INDEX_NAME and DEEN_FIQH_SPARSE_INDEX_NAME without a module-level guard.</done>
</task>

<task type="auto">
  <name>Task 3: Add .gitignore entry for BM25 encoder JSON and create data/ directory placeholder</name>
  <files>.gitignore, data/.gitkeep</files>
  <read_first>
    - .gitignore — read to find the correct section to add the data/*.json exclusion; check if a data/ entry already exists
  </read_first>
  <action>
1. Open .gitignore and add the following lines in a logical location (e.g., after any existing data file exclusions, or at the end of the file if no relevant section exists):

```
# Fiqh ingestion artifacts (generated, not committed)
data/*.json
```

2. Create the `data/` directory with a `.gitkeep` placeholder so the directory is tracked by git but empty:
   - Create file: `data/.gitkeep` (empty file)

The BM25 encoder will be saved to `data/fiqh_bm25_encoder.json` by the ingestion script. This file is ~50-200KB of derived data (document frequency map) that can be regenerated by re-running the ingestion script — it should not be committed.
  </action>
  <verify>
    <automated>grep "data/\*.json" /Users/shawn.n/Desktop/Deen/deen-backend/.gitignore && test -f /Users/shawn.n/Desktop/Deen/deen-backend/data/.gitkeep && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains the line `data/*.json`
    - `data/.gitkeep` file exists (may be empty)
    - `data/fiqh_bm25_encoder.json` would be gitignored (verify: `git check-ignore data/fiqh_bm25_encoder.json` outputs the path)
  </acceptance_criteria>
  <done>data/ directory exists with .gitkeep, and data/*.json is excluded from git.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Both new packages are findable in requirements.txt:
   `grep "pymupdf==1.27.2.2\|pinecone-text==0.11.0" requirements.txt` — should return 2 lines

2. Config exports are correct:
   `python3 -c "import sys; sys.path.insert(0, '.'); from core.config import DEEN_FIQH_DENSE_INDEX_NAME, DEEN_FIQH_SPARSE_INDEX_NAME; print('config OK')"` — should print "config OK" (value will be None if .env not set, which is fine)

3. gitignore works:
   `git check-ignore -v data/fiqh_bm25_encoder.json` — should output the .gitignore rule that matches
</verification>

<success_criteria>
- requirements.txt has pymupdf==1.27.2.2 and pinecone-text==0.11.0 (exact versions, pinned)
- core/config.py exports DEEN_FIQH_DENSE_INDEX_NAME and DEEN_FIQH_SPARSE_INDEX_NAME via os.getenv() with no module-level guard
- .gitignore excludes data/*.json
- data/.gitkeep exists so the directory is tracked
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
