---
phase: 08-config-dependencies
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
autonomous: true
requirements:
  - CONF-05
  - CONF-06
  - CONF-07

must_haves:
  truths:
    - "pip install -r requirements.txt installs langchain-anthropic, anthropic, and voyageai without error"
    - "requirements.txt does not contain langchain-openai or openai"
    - ".env.example documents ANTHROPIC_API_KEY and VOYAGE_API_KEY and does not contain OPENAI_API_KEY"
    - ".env.example LLM defaults reflect Claude model names"
    - ".env.example embedding defaults reflect voyage-4 and 1024"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned Anthropic + Voyage AI packages"
      contains: "langchain-anthropic==0.3.22"
    - path: ".env.example"
      provides: "Updated env template for v1.2"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "requirements.txt"
      to: "langchain-anthropic==0.3.22"
      via: "pip install"
      pattern: "langchain-anthropic==0.3.22"
---

<objective>
Update `requirements.txt` and `.env.example` to reflect the v1.2 provider swap: add `langchain-anthropic==0.3.22`, `anthropic==0.87.0`, and `voyageai==0.3.7` to requirements; remove `langchain-openai==0.3.25` and `openai==1.91.0`; update `.env.example` with the new API key section and model defaults.

Purpose: These files are the packaging contract and developer onboarding template. Getting them right ensures fresh installs work and new developers know which keys to configure.

Output: Updated `requirements.txt` (3 added, 2 removed) and `.env.example` (OpenAI section replaced with Anthropic + Voyage AI section, model defaults updated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-config-dependencies/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update requirements.txt — add Anthropic + Voyage AI packages, remove OpenAI packages</name>
  <read_first>
    - requirements.txt — full file (executor MUST read before editing to confirm line positions)
  </read_first>
  <files>requirements.txt</files>
  <action>
Make the following changes to `requirements.txt`. Read the file first to confirm exact lines before editing.

**Packages to REMOVE (2 lines):**
- Line 41: `langchain-openai==0.3.25` — remove this entire line
- Line 59: `openai==1.91.0` — remove this entire line

Do NOT remove `tiktoken==0.9.0` (line 108) — it is kept per D-02 because `scripts/ingest_fiqh.py` uses it directly.

**Packages to ADD (3 lines, alphabetically placed):**

1. `anthropic==0.87.0` — add after `annotated-types==0.7.0` (currently line 7). Insert as a new line between `annotated-types==0.7.0` and `anyio==4.8.0`.

2. `langchain-anthropic==0.3.22` — add after the `langchain==0.3.27` line (currently line 37). Insert between `langchain==0.3.27` and `langchain-community==0.3.27`.

3. `voyageai==0.3.7` — add after `vcrpy==7.0.0` (currently line 118). Insert between `vcrpy==7.0.0` and `wrapt==1.17.2`.

**Critical constraints:**
- Pin `anthropic==0.87.0` EXACTLY — not `0.92.0` (the version currently installed in venv). This is per D-05.
- `tiktoken==0.9.0` stays in the file — do NOT remove it.
- After all edits, verify there is exactly one entry each for `anthropic`, `langchain-anthropic`, `voyageai` and zero entries for `langchain-openai` and `openai`.

**Expected final state (spot-check):**
The file should contain these lines (exact versions):
```
anthropic==0.87.0
langchain-anthropic==0.3.22
voyageai==0.3.7
tiktoken==0.9.0
```
And must NOT contain:
```
langchain-openai
openai==
```
  </action>
  <verify>
    <automated>cd /Users/shawn.n/Desktop/Deen/deen-backend && python -c "
with open('requirements.txt') as f:
    lines = [l.strip() for l in f if l.strip()]
pkgs = {l.split('==')[0].lower(): l for l in lines if '==' in l}
assert 'anthropic' in pkgs and pkgs['anthropic'] == 'anthropic==0.87.0', f'anthropic wrong: {pkgs.get(\"anthropic\")}'
assert 'langchain-anthropic' in pkgs and pkgs['langchain-anthropic'] == 'langchain-anthropic==0.3.22', f'langchain-anthropic wrong: {pkgs.get(\"langchain-anthropic\")}'
assert 'voyageai' in pkgs and pkgs['voyageai'] == 'voyageai==0.3.7', f'voyageai wrong: {pkgs.get(\"voyageai\")}'
assert 'tiktoken' in pkgs, 'tiktoken was removed — must be kept'
assert 'langchain-openai' not in pkgs, 'langchain-openai should be removed'
assert 'openai' not in pkgs, f'openai should be removed, found: {pkgs.get(\"openai\")}'
print('requirements.txt assertions PASSED')
"
</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains: `anthropic==0.87.0`
    - requirements.txt contains: `langchain-anthropic==0.3.22`
    - requirements.txt contains: `voyageai==0.3.7`
    - requirements.txt contains: `tiktoken==0.9.0`
    - requirements.txt does NOT contain: `langchain-openai`
    - requirements.txt does NOT contain: `openai==1.91.0` (or any `openai==` line)
    - The verify script above exits 0 and prints "requirements.txt assertions PASSED"
  </acceptance_criteria>
  <done>requirements.txt lists langchain-anthropic==0.3.22, anthropic==0.87.0, voyageai==0.3.7; langchain-openai and openai are removed; tiktoken remains.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update .env.example — replace OpenAI section with Anthropic + Voyage AI section</name>
  <read_first>
    - .env.example — full file (executor MUST read before editing to see current content)
  </read_first>
  <files>.env.example</files>
  <action>
Make the following changes to `.env.example`. Read the file first before editing.

**Change 1 — Replace the OpenAI section (lines 8-11) with Anthropic + Voyage AI section:**

Remove:
```bash
# === OpenAI ===
OPENAI_API_KEY=your-openai-api-key-here
LARGE_LLM=gpt-4.1-2025-04-14
SMALL_LLM=gpt-4o-mini-2024-07-18
```

Replace with:
```bash
# === Anthropic + Voyage AI ===
ANTHROPIC_API_KEY=your-anthropic-api-key-here
VOYAGE_API_KEY=your-voyage-api-key-here
LARGE_LLM=claude-sonnet-4-6
SMALL_LLM=claude-haiku-4-5-20251001
```

**Change 2 — Update the Memory / Personalization section (lines 52-54):**

Replace:
```bash
EMBEDDING_MODEL=text-embedding-3-small
# Dimension count for the embedding model above
EMBEDDING_DIMENSIONS=1536
```

With:
```bash
EMBEDDING_MODEL=voyage-4
# Dimension count for the embedding model above
EMBEDDING_DIMENSIONS=1024
```

**Change 3 — Update the file header comment (line 4):**

The current line 4 reads:
```
# As of v1.1: COGNITO_REGION and COGNITO_POOL_ID are REMOVED.
```

Update to add a v1.2 note after the v1.1 note:
```
# As of v1.1: COGNITO_REGION and COGNITO_POOL_ID are REMOVED.
#             SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY replace them.
# As of v1.2: OPENAI_API_KEY is REMOVED. ANTHROPIC_API_KEY and VOYAGE_API_KEY replace it.
```

(Note: line 5 currently says `#             SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY replace them.` — keep it and add the v1.2 line after it.)

**No other changes.** Pinecone section, Supabase section, Database section, Redis section, and App section remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/shawn.n/Desktop/Deen/deen-backend && python -c "
with open('.env.example') as f:
    content = f.read()
assert 'ANTHROPIC_API_KEY=your-anthropic-api-key-here' in content, 'ANTHROPIC_API_KEY entry missing'
assert 'VOYAGE_API_KEY=your-voyage-api-key-here' in content, 'VOYAGE_API_KEY entry missing'
assert 'LARGE_LLM=claude-sonnet-4-6' in content, 'LARGE_LLM default wrong'
assert 'SMALL_LLM=claude-haiku-4-5-20251001' in content, 'SMALL_LLM default wrong'
assert 'EMBEDDING_MODEL=voyage-4' in content, 'EMBEDDING_MODEL default wrong'
assert 'EMBEDDING_DIMENSIONS=1024' in content, 'EMBEDDING_DIMENSIONS default wrong'
assert 'OPENAI_API_KEY' not in content, 'OPENAI_API_KEY should be removed'
assert 'gpt-4.1' not in content, 'old gpt-4.1 default should be removed'
assert 'gpt-4o-mini' not in content, 'old gpt-4o-mini default should be removed'
assert 'text-embedding-3-small' not in content, 'old embedding model should be removed'
assert '1536' not in content, 'old embedding dimensions should be removed'
assert 'v1.2' in content, 'v1.2 header comment missing'
print('.env.example assertions PASSED')
"
</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains: `ANTHROPIC_API_KEY=your-anthropic-api-key-here`
    - .env.example contains: `VOYAGE_API_KEY=your-voyage-api-key-here`
    - .env.example contains: `LARGE_LLM=claude-sonnet-4-6`
    - .env.example contains: `SMALL_LLM=claude-haiku-4-5-20251001`
    - .env.example contains: `EMBEDDING_MODEL=voyage-4`
    - .env.example contains: `EMBEDDING_DIMENSIONS=1024`
    - .env.example does NOT contain: `OPENAI_API_KEY`
    - .env.example does NOT contain: `gpt-4.1` or `gpt-4o-mini`
    - .env.example does NOT contain: `text-embedding-3-small` or `1536`
    - The verify script above exits 0 and prints ".env.example assertions PASSED"
  </acceptance_criteria>
  <done>.env.example documents ANTHROPIC_API_KEY and VOYAGE_API_KEY, has Claude model defaults, has voyage-4 embedding defaults, and contains no OpenAI references.</done>
</task>

</tasks>

<verification>
Run both verify scripts from Tasks 1 and 2. Then run a combined sanity check:

```bash
cd /Users/shawn.n/Desktop/Deen/deen-backend && grep -c "openai" requirements.txt && echo "WARNING: openai still in requirements.txt" || echo "openai clean from requirements.txt"
grep -c "OPENAI" .env.example && echo "WARNING: OPENAI still in .env.example" || echo "OPENAI clean from .env.example"
```

Both commands should return 0 matches (exit 1 from grep with no matches = clean).
</verification>

<success_criteria>
- `pip install -r requirements.txt` installs `langchain-anthropic==0.3.22`, `anthropic==0.87.0`, `voyageai==0.3.7` (CONF-05)
- `langchain-openai` and `openai` are absent from `requirements.txt`; `tiktoken` remains (CONF-06)
- `.env.example` has `ANTHROPIC_API_KEY` + `VOYAGE_API_KEY` entries, Claude LLM defaults, voyage-4 embedding defaults, and no OpenAI references (CONF-07)
</success_criteria>

<output>
After completion, create `.planning/phases/08-config-dependencies/08-02-SUMMARY.md`
</output>
