# Phase 10: Embedding Migration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10
**Phase:** 10-embedding-migration
**Areas discussed:** Provider, Config guard, Migration approach, Backfill script, Model reuse

---

## Embedding Provider

| Option | Description | Selected |
|--------|-------------|----------|
| HuggingFace all-mpnet-base-v2 | 768-dim, already installed (sentence-transformers), no API key. Matches PROJECT.md decision. | ✓ |
| Voyage AI voyage-4 | 1024-dim, needs VOYAGE_API_KEY. Matched REQUIREMENTS.md as written. | |

**User's choice:** HuggingFace all-mpnet-base-v2
**Notes:** PROJECT.md had already recorded this decision after Phase 9. REQUIREMENTS.md and ROADMAP.md still said Voyage AI — these will be updated by the planner.

---

## Config Guard Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 10 | Remove VOYAGE_API_KEY guard and env var in this phase — directly related to the embedding swap. | ✓ |
| Phase 11 (Dead Code Cleanup) | Leave it for cleanup phase. Guard wouldn't cause harm even without a key. | |

**User's choice:** Phase 10
**Notes:** Clean removal in the same phase that makes VOYAGE_API_KEY irrelevant. voyageai package in requirements.txt stays for Phase 11.

---

## Migration Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Add-column swap | Add embedding_new → backfill → drop old → rename. Safe for prod with existing rows. Matches EMBED-05 spec. | |
| DROP + recreate tables | Drop embedding tables entirely, recreate at 768-dim. Fine since there are no prod rows worth keeping. | ✓ |

**User's choice:** DROP + recreate tables
**Notes:** Both tables are fully regenerated by the backfill script anyway. Simpler migration with no intermediate column state.

---

## Backfill Script Identity

| Option | Description | Selected |
|--------|-------------|----------|
| Update existing script | Rename migrate_embeddings.py → reembed_pgvector.py, update to new provider. Git history preserved. | ✓ |
| Create new reembed_pgvector.py | Fresh script, leave migrate_embeddings.py as dead code for Phase 11 to clean. | |

**User's choice:** Update existing script (rename + update)
**Notes:** Uses `git mv` so rename is tracked. Existing logic (lesson + note migration, batch commit, stats) is preserved.

---

## Model Instance Reuse

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse getDenseEmbedder() | EmbeddingService imports from modules.embedding.embedder. Model loads once, shared with Pinecone retrieval path. | ✓ |
| Own instance | EmbeddingService loads its own HuggingFaceEmbeddings in __init__. Independent but doubles the 400MB load. | |

**User's choice:** Reuse getDenseEmbedder()
**Notes:** modules/embedding/embedder.py already loads sentence_transformer at module level. getDenseEmbedder() returns it. No reason to load twice.

---

## Claude's Discretion

- Whether EMBEDDING_MODEL env var remains relevant after the swap (could be hardcoded)
- Exact revision ID and description for the new Alembic migration file
- CHUNK_SIZE/CHUNK_OVERLAP constants (256/50 tokens — conservative, fine for all-mpnet-base-v2's 512-token max)

## Deferred Ideas

None — discussion stayed within phase scope.
