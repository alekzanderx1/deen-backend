---
phase: 07-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - api/account.py
  - .env.example
  - README.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "grep -r boto3 . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude returns no matches"
    - "A fresh operator can copy .env.example to .env and know exactly what each variable is and where to get it"
    - "README.md contains no references to COGNITO_REGION, COGNITO_POOL_ID, or AWS Cognito authentication setup"
    - "README.md documents the v1.0 → v1.1 migration: Cognito vars removed, Supabase vars added"
  artifacts:
    - path: "requirements.txt"
      provides: "Dependency list with boto3 removed"
      contains_not: "boto3"
    - path: "api/account.py"
      provides: "Account management endpoints without boto3/botocore imports"
      contains_not: "import boto3"
    - path: ".env.example"
      provides: "Machine-readable environment variable template"
      exports: ["OPENAI_API_KEY", "SUPABASE_URL", "DATABASE_URL", "REDIS_URL"]
    - path: "README.md"
      provides: "Human-readable env var documentation section"
      contains: "Environment Variables"
  key_links:
    - from: "requirements.txt"
      to: "Dockerfile"
      via: "pip install -r requirements.txt"
      pattern: "No boto3 in requirements.txt = no boto3 in Docker image"
    - from: ".env.example"
      to: "core/config.py"
      via: "Every os.getenv() call in config.py must have a corresponding entry in .env.example"
---

<objective>
Remove all boto3/AWS dependencies from the application and produce complete environment variable documentation for fresh deployments.

Purpose: This is the final cleanup phase of the Supabase migration (v1.1). boto3 is dead code — Phase 6 replaced the only boto3 call (Cognito AdminDeleteUser) with an httpx call to the Supabase Admin API. Leaving dead imports and a dead dependency risks runtime errors once boto3 is absent from the deployment environment. Documentation ensures operators deploying fresh do not need git history to understand the current env var set.

Output:
- requirements.txt with boto3==1.35.96 removed (CLEAN-01)
- api/account.py with import boto3 and from botocore.exceptions import ClientError removed (CLEAN-01)
- .env.example created at repo root with all required vars grouped by service (CLEAN-02)
- README.md updated: old Cognito env block replaced, new dedicated Environment Variables section added, stale Cognito references in Troubleshooting fixed (CLEAN-02)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cleanup/07-CONTEXT.md
@.planning/phases/07-cleanup/07-RESEARCH.md
</context>

<interfaces>
<!-- Authoritative env var source. Every variable in this list must appear in .env.example. -->
<!-- Extracted from core/config.py (lines 1-94) and db/config.py -->

From core/config.py — all os.getenv() calls:
  ENV                         (default: "development")
  OPENAI_API_KEY              (required — ValueError guard)
  PINECONE_API_KEY            (required — ValueError guard)
  DEEN_DENSE_INDEX_NAME       (required — ValueError guard)
  DEEN_SPARSE_INDEX_NAME      (required — ValueError guard)
  QURAN_DENSE_INDEX_NAME      (optional)
  DEEN_FIQH_DENSE_INDEX_NAME  (optional — no server-level guard)
  DEEN_FIQH_SPARSE_INDEX_NAME (optional — no server-level guard)
  DENSE_RESULT_WEIGHT         (default: 0.8)
  SPARSE_RESULT_WEIGHT        (default: 0.2)
  REFERENCE_FETCH_COUNT       (default: 10)
  REDIS_URL                   (default: "redis://localhost:6379/0")
  REDIS_KEY_PREFIX            (env name: REDIS_KEY_PREFIX, default: "dev:chat")
  REDIS_TTL_SECONDS           (default: 12000)
  REDIS_MAX_MESSAGES          (default: 30)
  SUPABASE_URL                (required — startup guard via validate_supabase_config())
  SUPABASE_SERVICE_ROLE_KEY   (required — startup guard via validate_supabase_config())
  LARGE_LLM                   (no default — required in practice)
  SMALL_LLM                   (no default — required in practice)
  DATABASE_URL                (or built from DB_* components)
  ASYNC_DATABASE_URL          (or built from DB_* components)
  DB_HOST                     (component alternative to DATABASE_URL)
  DB_PORT                     (default: 5432)
  DB_NAME                     (component alternative)
  DB_USER                     (component alternative)
  DB_PASSWORD                 (component alternative)
  EMBEDDING_MODEL             (default: "text-embedding-3-small")
  EMBEDDING_DIMENSIONS        (default: 1536)
  NOTE_FILTER_THRESHOLD       (default: 0.4)
  SIGNAL_QUALITY_THRESHOLD    (default: 0.5)

CORS_ALLOW_ORIGINS — loaded in main.py via os.getenv("CORS_ALLOW_ORIGINS", "*")

Vars explicitly REMOVED as of v1.1 (do NOT include in .env.example):
  COGNITO_REGION
  COGNITO_POOL_ID

From api/account.py current import block (lines 8-19):
  import boto3                              ← REMOVE (line 10)
  from botocore.exceptions import ClientError ← REMOVE (line 11)
  (all other imports stay)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove boto3 from requirements.txt and api/account.py</name>
  <files>requirements.txt, api/account.py</files>

  <read_first>
    - /Users/shawn.n/Desktop/Deen/deen-backend/requirements.txt — confirm line 8 is `boto3==1.35.96`; confirm no other boto-named lines exist
    - /Users/shawn.n/Desktop/Deen/deen-backend/api/account.py — confirm lines 10-11 are the two boto imports; confirm no boto3 or ClientError symbol appears in any function body below line 19
  </read_first>

  <action>
    Step 1 — Edit requirements.txt:
    Remove the single line `boto3==1.35.96` (line 8). Do not remove any other lines. The transitive dependencies (botocore, s3transfer, jmespath) are not listed explicitly in requirements.txt; they do not need to be manually removed.

    Step 2 — Edit api/account.py:
    Remove exactly these two lines from the import block:
      import boto3
      from botocore.exceptions import ClientError

    The import block after removal must be:
      from fastapi import APIRouter, Depends, HTTPException
      from sqlalchemy.orm import Session
      import httpx
      import logging

    Do NOT remove any other lines. Do NOT modify any function bodies — the boto3 call was already replaced with httpx in Phase 6.

    Step 3 — Verify no boto3 traces remain:
    Run: grep -r boto3 . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude
    Expected output: empty (no matches).

    Also run: grep -r botocore . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude
    Expected output: empty (no matches).
  </action>

  <verify>
    <automated>grep -r boto3 /Users/shawn.n/Desktop/Deen/deen-backend --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude; echo "Exit: $?"</automated>
    <automated>grep -r botocore /Users/shawn.n/Desktop/Deen/deen-backend --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude; echo "Exit: $?"</automated>
    <automated>grep -n "import boto" /Users/shawn.n/Desktop/Deen/deen-backend/api/account.py; echo "Exit: $?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -r boto3 . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude` returns no output (zero matches)
    - `grep -r botocore . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude` returns no output (zero matches)
    - requirements.txt does not contain the string `boto3`
    - api/account.py line 10 is `import httpx` (not `import boto3`)
    - api/account.py does not contain the string `ClientError`
  </acceptance_criteria>

  <done>boto3 and botocore are fully absent from application code and dependency files. grep confirms zero matches across the repo (excluding .git, venv, .claude).</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example and update README.md env documentation</name>
  <files>.env.example, README.md</files>

  <read_first>
    - /Users/shawn.n/Desktop/Deen/deen-backend/README.md — read the full file; locate the Quick Start "Configure Environment Variables" block (lines 45-84, the inline .env code block with COGNITO vars); locate the Troubleshooting "Authentication Errors" entry (line 360); locate the Authentication documentation link (line 140)
    - /Users/shawn.n/Desktop/Deen/deen-backend/core/config.py — confirm the complete list of os.getenv() calls (the interfaces block above was extracted from this file; verify it is still accurate before writing .env.example)
  </read_first>

  <action>
    Step 1 — Create .env.example at repo root:

    Write the following content exactly. Placeholder values are intentionally obvious. Vars with safe defaults show the default value. Required vars with no safe default show a placeholder string ending in `-here`.

    ```
    # =============================================================================
    # Deen Backend — Environment Variables Template
    # Copy this file to .env and fill in the real values.
    # As of v1.1: COGNITO_REGION and COGNITO_POOL_ID are REMOVED.
    #             SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY replace them.
    # =============================================================================

    # === OpenAI ===
    OPENAI_API_KEY=your-openai-api-key-here
    LARGE_LLM=gpt-4.1-2025-04-14
    SMALL_LLM=gpt-4o-mini-2024-07-18

    # === Pinecone ===
    PINECONE_API_KEY=your-pinecone-api-key-here
    # General Islamic content indexes (hadith, Quran)
    DEEN_DENSE_INDEX_NAME=deen-dense
    DEEN_SPARSE_INDEX_NAME=deen-sparse
    QURAN_DENSE_INDEX_NAME=quran-dense
    # Fiqh-specific indexes (Sistani rulings) — optional at server startup; required for fiqh queries
    DEEN_FIQH_DENSE_INDEX_NAME=deen-fiqh-dense
    DEEN_FIQH_SPARSE_INDEX_NAME=deen-fiqh-sparse
    # Retrieval weights (must sum to 1.0)
    DENSE_RESULT_WEIGHT=0.8
    SPARSE_RESULT_WEIGHT=0.2
    REFERENCE_FETCH_COUNT=10

    # === Supabase (replaces AWS Cognito as of v1.1) ===
    # Project URL — found in Supabase Dashboard > Project Settings > API
    SUPABASE_URL=https://xxxx.supabase.co
    # Service role key (secret) — Supabase Dashboard > Project Settings > API > service_role
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

    # === Database (Supabase Postgres — direct connection, port 5432) ===
    # Use the direct connection string, NOT the transaction pooler (port 6543 is incompatible with asyncpg)
    DATABASE_URL=postgresql://postgres.xxxx:your-db-password-here@aws-0-us-east-1.pooler.supabase.com:5432/postgres
    ASYNC_DATABASE_URL=postgresql+asyncpg://postgres.xxxx:your-db-password-here@aws-0-us-east-1.pooler.supabase.com:5432/postgres
    # Individual components (alternative to DATABASE_URL — used if DATABASE_URL is not set)
    DB_HOST=aws-0-us-east-1.pooler.supabase.com
    DB_PORT=5432
    DB_NAME=postgres
    DB_USER=postgres.xxxx
    DB_PASSWORD=your-db-password-here

    # === Redis ===
    REDIS_URL=redis://localhost:6379/0
    REDIS_KEY_PREFIX=dev:chat
    REDIS_TTL_SECONDS=12000
    REDIS_MAX_MESSAGES=30

    # === App ===
    # ENV controls dev bypass behavior: "development" enables auth bypass for local testing
    ENV=development
    CORS_ALLOW_ORIGINS=http://localhost:3000
    ```

    Step 2 — Update README.md:

    2a. Replace the Quick Start "Configure Environment Variables" section (step 3, lines ~45-84) which currently contains the old inline `.env` block with COGNITO vars. Replace the entire inline code block inside that step with:

    ```bash
    cp .env.example .env
    # Edit .env and fill in your real values.
    # See the "Environment Variables" section below for descriptions of each variable.
    ```

    Keep the step heading "### 3. Configure Environment Variables" and the line "Create a `.env` file in the root directory with the following variables:" — but replace "with the following variables:" with "by copying the template:" and replace the entire code block with the cp command above.

    2b. Update the Troubleshooting section "Authentication Errors" block (around line 360). Replace:
    ```
    - Check Cognito configuration in `.env`
    ```
    with:
    ```
    - Check `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are set in `.env`
    - Verify the Supabase JWT signing key is asymmetric (RS256/ES256): `curl <SUPABASE_URL>/auth/v1/keys` should return a non-empty `keys` array
    ```

    2c. Update the Authentication documentation link (around line 140). Replace:
    ```
    - [**Authentication**](documentation/AUTHENTICATION.md) - AWS Cognito JWT authentication setup
    ```
    with:
    ```
    - [**Authentication**](documentation/AUTHENTICATION.md) - Supabase Auth JWT authentication setup (v1.1+)
    ```

    2d. Update Prerequisites section to replace "AWS Cognito" with "Supabase account (for authentication and database)". The current Prerequisites list mentions "PostgreSQL database" — update to "Supabase account (Postgres + Auth)" since Supabase provides both.

    2e. Add a new "## Environment Variables" section to README.md. Place it after the "## Key Technologies" section (around line 152) and before "## Development Tools". Content:

    ```markdown
    ## Environment Variables

    Copy `.env.example` to `.env` and fill in the real values. All variables are described below.

    > **Upgrading from v1.0?** Remove `COGNITO_REGION` and `COGNITO_POOL_ID` from your `.env` — these are no longer used. Add `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` instead.

    ### OpenAI

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `OPENAI_API_KEY` | Yes | OpenAI API key. Get from [platform.openai.com/api-keys](https://platform.openai.com/api-keys) |
    | `LARGE_LLM` | Yes | Large model ID for generation, filtering, refinement. Default: `gpt-4.1-2025-04-14` |
    | `SMALL_LLM` | Yes | Small model ID for classification, routing, decomposition. Default: `gpt-4o-mini-2024-07-18` |

    ### Pinecone

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `PINECONE_API_KEY` | Yes | Pinecone API key. Get from [app.pinecone.io](https://app.pinecone.io) |
    | `DEEN_DENSE_INDEX_NAME` | Yes | Dense vector index for hadith/Islamic content |
    | `DEEN_SPARSE_INDEX_NAME` | Yes | Sparse vector index for hadith/Islamic content |
    | `QURAN_DENSE_INDEX_NAME` | No | Dense vector index for Quran tafsir |
    | `DEEN_FIQH_DENSE_INDEX_NAME` | No* | Dense vector index for Sistani fiqh rulings. *Required for fiqh queries |
    | `DEEN_FIQH_SPARSE_INDEX_NAME` | No* | Sparse vector index for Sistani fiqh rulings. *Required for fiqh queries |
    | `DENSE_RESULT_WEIGHT` | No | Weight for dense retrieval results (default: `0.8`). Must sum to 1.0 with `SPARSE_RESULT_WEIGHT` |
    | `SPARSE_RESULT_WEIGHT` | No | Weight for sparse retrieval results (default: `0.2`) |
    | `REFERENCE_FETCH_COUNT` | No | Number of references to fetch per query (default: `10`) |

    ### Supabase

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `SUPABASE_URL` | Yes | Project URL. Supabase Dashboard → Project Settings → API |
    | `SUPABASE_SERVICE_ROLE_KEY` | Yes | Service role secret key. Supabase Dashboard → Project Settings → API → `service_role` |

    ### Database

    Provide either `DATABASE_URL` / `ASYNC_DATABASE_URL` directly, or provide all `DB_*` components and the app will build the URL.

    Use the **direct connection** (port 5432), not the transaction pooler (port 6543 is incompatible with asyncpg).

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `DATABASE_URL` | Yes* | Sync PostgreSQL connection string (`postgresql://...`) |
    | `ASYNC_DATABASE_URL` | Yes* | Async PostgreSQL connection string (`postgresql+asyncpg://...`) |
    | `DB_HOST` | Yes* | Database host (alternative to `DATABASE_URL`) |
    | `DB_PORT` | No | Database port (default: `5432`) |
    | `DB_NAME` | Yes* | Database name (alternative to `DATABASE_URL`) |
    | `DB_USER` | Yes* | Database user (alternative to `DATABASE_URL`) |
    | `DB_PASSWORD` | Yes* | Database password (alternative to `DATABASE_URL`) |

    *Either `DATABASE_URL` + `ASYNC_DATABASE_URL`, or all `DB_*` components must be provided.

    ### Redis

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `REDIS_URL` | No | Redis connection URL (default: `redis://localhost:6379/0`). Falls back to in-process ephemeral history if unreachable |
    | `REDIS_KEY_PREFIX` | No | Namespace prefix for Redis keys (default: `dev:chat`) |
    | `REDIS_TTL_SECONDS` | No | Conversation TTL in seconds (default: `12000` ≈ 3.3 hours) |
    | `REDIS_MAX_MESSAGES` | No | Max messages kept per session (default: `30`) |

    ### App

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `ENV` | No | Runtime environment. `development` enables auth bypass for local testing (default: `development`) |
    | `CORS_ALLOW_ORIGINS` | No | Comma-separated list of allowed CORS origins (default: `*`) |
    ```
  </action>

  <verify>
    <automated>test -f /Users/shawn.n/Desktop/Deen/deen-backend/.env.example && echo "EXISTS" || echo "MISSING"</automated>
    <automated>grep -n "SUPABASE_URL" /Users/shawn.n/Desktop/Deen/deen-backend/.env.example</automated>
    <automated>grep -n "COGNITO" /Users/shawn.n/Desktop/Deen/deen-backend/.env.example; echo "Cognito lines (must be 0): $?"</automated>
    <automated>grep -n -i "cognito" /Users/shawn.n/Desktop/Deen/deen-backend/README.md; echo "Above: Cognito refs remaining (should be 0 or only in migration callout)"</automated>
    <automated>grep -n "Environment Variables" /Users/shawn.n/Desktop/Deen/deen-backend/README.md</automated>
    <automated>grep -n "Upgrading from v1.0" /Users/shawn.n/Desktop/Deen/deen-backend/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `.env.example` exists at repo root (`test -f .env.example` exits 0)
    - `.env.example` contains the string `SUPABASE_URL=https://xxxx.supabase.co`
    - `.env.example` contains the string `DEEN_FIQH_DENSE_INDEX_NAME`
    - `.env.example` contains the string `DEEN_FIQH_SPARSE_INDEX_NAME`
    - `.env.example` does NOT contain the string `COGNITO` (grep returns no matches)
    - `grep -n -i "cognito" README.md` returns zero matches (or only appears inside the "Upgrading from v1.0" migration callout, not in any setup instruction)
    - `grep -n "Upgrading from v1.0" README.md` returns at least one match
    - `grep -n "## Environment Variables" README.md` returns at least one match
    - `grep -n "SUPABASE_URL" README.md` returns at least one match (in the new env vars table)
    - README.md Quick Start step 3 does NOT contain a raw inline `.env` code block with `COGNITO_REGION` in it
    - README.md Troubleshooting "Authentication Errors" references `SUPABASE_URL`, not "Cognito configuration"
  </acceptance_criteria>

  <done>.env.example is created with all 25+ vars grouped by service, using placeholder values for secrets and default values for optional vars. README.md has a new "## Environment Variables" section, the Quick Start env block is replaced with a reference to .env.example, and all stale Cognito references in Troubleshooting and Prerequisites are updated. grep -i "cognito" README.md returns zero matches outside the migration callout.</done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks:

```bash
# CLEAN-01: boto3 fully absent
grep -r boto3 . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude
grep -r botocore . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude
# Both must return empty output

# CLEAN-02: Documentation complete
test -f .env.example && echo "OK: .env.example exists"
grep "SUPABASE_URL" .env.example && echo "OK: Supabase vars present"
grep "DEEN_FIQH_DENSE_INDEX_NAME" .env.example && echo "OK: fiqh vars present"
grep -c "COGNITO" .env.example && echo "FAIL: COGNITO in .env.example" || echo "OK: no COGNITO in .env.example"
grep "## Environment Variables" README.md && echo "OK: env section added"
grep "Upgrading from v1.0" README.md && echo "OK: migration callout present"
grep -i "COGNITO_REGION" README.md && echo "FAIL: stale COGNITO_REGION still in README" || echo "OK: COGNITO_REGION removed"
```
</verification>

<success_criteria>
1. `grep -r boto3 . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude` returns no output — boto3 is absent from all application code and config files.
2. `grep -r botocore . --exclude-dir=.git --exclude-dir=venv --exclude-dir=.claude` returns no output — botocore import is also gone.
3. `.env.example` exists at repo root with all required vars from core/config.py grouped under service headers (OpenAI, Pinecone, Supabase, Database, Redis, App), using placeholder values for secrets.
4. `README.md` contains a `## Environment Variables` section with tables describing each variable group.
5. `README.md` contains the text "Upgrading from v1.0" directing operators to remove COGNITO_REGION and COGNITO_POOL_ID.
6. `grep -i "COGNITO_REGION" README.md` returns no matches.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-01-SUMMARY.md` using the summary template.
</output>
