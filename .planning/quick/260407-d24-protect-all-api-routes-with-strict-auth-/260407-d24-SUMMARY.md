---
phase: quick
plan: 260407-d24
subsystem: auth
tags: [auth, security, jwt, supabase, dev-bypass]

# Dependency graph
requires: []
provides:
  - "DevBypassBearer class in models/JWTBearer.py"
  - "Single auth dependency (ENV-aware) in core/auth.py"
  - "Strict JWT auth on all API routes"
  - "ENV constant in core/config.py"
  - "SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY constants in core/config.py"
affects: [api/chat.py, api/reference.py, api/hikmah.py, api/primers.py, api/memory_admin.py, api/account.py]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "DevBypassBearer: dev bypass (no token = mock claims), prod strict (delegates to JWTBearer)"
    - "Single auth export pattern: all routes import only `auth` from core.auth"

key-files:
  created: []
  modified:
    - models/JWTBearer.py
    - core/auth.py
    - core/config.py
    - api/chat.py
    - api/reference.py
    - api/hikmah.py
    - api/primers.py
    - api/memory_admin.py
    - api/account.py

key-decisions:
  - "DevBypassBearer delegates to JWTBearer when Bearer token is present even in dev — real tokens are always validated"
  - "ENV defaults to 'development' — safe for local work without requiring env var setup"
  - "SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY forward-ported from Phase 06 P01 — worktree was on pre-migration state"
  - "api/account.py forward-ported from Phase 06 P03 — required to fix COGNITO_REGION import breakage"

# Metrics
duration: 9min
completed: 2026-04-07
---

# Quick Task 260407-d24: Protect All API Routes with Strict Auth — Summary

**DevBypassBearer class enforces JWT auth on all routes: dev mode returns mock claims {sub: dev-user-001} when no token is provided; production mode requires valid Supabase JWT**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-04-07T13:26:57Z
- **Completed:** 2026-04-07T13:35:10Z
- **Tasks:** 2 of 2
- **Files modified:** 9

## Accomplishments

- Added `ENV` constant to `core/config.py` (defaults to `"development"`)
- Added `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` to `core/config.py` (forward-port from Phase 06 P01)
- Appended `DevBypassBearer` class to `models/JWTBearer.py` — skips JWKS verification in dev, delegates to strict `JWTBearer` in production
- Replaced `core/auth.py` to fetch Supabase JWKS and export single `auth = DevBypassBearer(jwks, env=ENV)` dependency
- Removed `optional_auth` from `api/chat.py` and applied `Depends(auth)` to all 5 chat routes (/, /stream, /stream/agentic, /agentic, /session/{id})
- Added `Depends(auth)` to `POST /references/` in `api/reference.py`
- Added `Depends(auth)` to all 9 handlers in `api/hikmah.py`
- Added `Depends(auth)` to all 3 handlers in `api/primers.py`
- Added `Depends(auth)` to all 5 handlers in `api/memory_admin.py`
- Forward-ported Phase 06 P03 `api/account.py` to replace Cognito imports with Supabase imports

## Task Commits

Each task was committed atomically:

1. **Task 1: Add DevBypassBearer + update core/auth.py** — `30e1478` (feat)
2. **Task 2: Apply strict auth to all unprotected routes** — `3841827` (feat)

## Files Created/Modified

- `core/config.py` — Added `ENV`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` constants; removed `COGNITO_REGION`/`COGNITO_POOL_ID`
- `models/JWTBearer.py` — Appended `DevBypassBearer` class
- `core/auth.py` — Replaced Cognito JWKS fetch with Supabase; exports single `auth` (DevBypassBearer instance)
- `api/chat.py` — Removed `optional_auth`, applied `Depends(auth)` to all 5 routes
- `api/reference.py` — Added auth imports and `Depends(auth)` to POST /references/
- `api/hikmah.py` — Added auth imports and `Depends(auth)` to all 9 route handlers
- `api/primers.py` — Added auth imports and `Depends(auth)` to all 3 route handlers
- `api/memory_admin.py` — Added auth imports and `Depends(auth)` to all 5 route handlers
- `api/account.py` — Forward-ported from Phase 06 P03 (Supabase account deletion)

## Decisions Made

- `DevBypassBearer` falls back to strict validation when a Bearer token *is* provided in dev — this ensures developers testing with real tokens still get proper validation
- `ENV` defaults to `"development"` — safe, doesn't require env var configuration for local work
- Forward-ported Phase 06 P01 config constants and Phase 06 P03 account.py — worktree was on pre-migration state

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] SUPABASE_URL missing from core/config.py**
- **Found during:** Task 1 (after writing core/auth.py which imports SUPABASE_URL)
- **Issue:** Worktree was on pre-Phase-06-P01 state; `core/config.py` still had `COGNITO_REGION`/`COGNITO_POOL_ID` instead of Supabase vars
- **Fix:** Added `SUPABASE_URL = os.getenv("SUPABASE_URL")` and `SUPABASE_SERVICE_ROLE_KEY = os.getenv("SUPABASE_SERVICE_ROLE_KEY")` to `core/config.py`; removed `COGNITO_REGION`/`COGNITO_POOL_ID`
- **Files modified:** `core/config.py`
- **Commit:** 30e1478 (Task 1)

**2. [Rule 3 - Blocking] api/account.py imported COGNITO_REGION (broken after config change)**
- **Found during:** Task 2 import verification
- **Issue:** Worktree's `api/account.py` was on pre-Phase-06-P03 state, importing `COGNITO_REGION, COGNITO_POOL_ID` which no longer exist in config.py
- **Fix:** Forward-ported Phase 06 P03 version of `api/account.py` — uses Supabase Admin API for account deletion, imports `SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY`
- **Files modified:** `api/account.py`
- **Commit:** 3841827 (Task 2)

## Test Results

- Pre-existing failures: 6 (test_fiqh_integration, test_primer_service — confirmed pre-existing on main branch)
- New failures introduced: 0
- Total passing: 198 of 204 tests

## Self-Check: PASSED

- `models/JWTBearer.py` — DevBypassBearer class at line 76: FOUND
- `core/auth.py` — exports `auth` (DevBypassBearer): FOUND
- `core/config.py` — ENV constant: FOUND
- `grep optional_auth api/` — Clean (no matches)
- All route module imports: PASSED
- Commits 30e1478 and 3841827: FOUND
