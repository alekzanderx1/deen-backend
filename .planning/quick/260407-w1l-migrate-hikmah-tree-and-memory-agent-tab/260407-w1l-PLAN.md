---
phase: quick
plan: 260407-w1l
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/20260407_create_memory_agent_tables.py
  - agents/models/user_memory_models.py
autonomous: true
requirements: [DB-02]

must_haves:
  truths:
    - "Running `alembic upgrade head` on a fresh Supabase DB creates user_memory_profiles, memory_events, and memory_consolidations tables"
    - "The three memory tables are consistent with the SQLAlchemy models used by MemoryProfileRepository, MemoryEventRepository, and MemoryConsolidationRepository"
    - "The migration appends cleanly to the existing chain (down_revision = chat_history_001)"
  artifacts:
    - path: "alembic/versions/20260407_create_memory_agent_tables.py"
      provides: "Alembic migration creating user_memory_profiles, memory_events, memory_consolidations"
      contains: "revision = 'memory_agent_001'"
  key_links:
    - from: "alembic/versions/20260407_create_memory_agent_tables.py"
      to: "agents/models/user_memory_models.py"
      via: "Column definitions match SQLAlchemy model fields"
      pattern: "user_memory_profiles.*memory_events.*memory_consolidations"
---

<objective>
Create the missing Alembic migration for the three memory agent tables — user_memory_profiles, memory_events, and memory_consolidations — so that `alembic upgrade head` fully provisions Supabase.

Purpose: The hikmah tree itself (hikmah_trees table) is already covered in the genesis migration (initial_schema_001). The truly missing tables are the three owned by agents/models/user_memory_models.py. These live in a separate declarative_base from db/session.py, so Alembic's env.py (which imports only db.session.Base) has never generated a migration for them.

Output: One new migration file at the tail of the chain (after chat_history_001) that creates the three missing tables, matching the column definitions in agents/models/user_memory_models.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260407-w1l-migrate-hikmah-tree-and-memory-agent-tab/260407-w1l-PLAN.md

<gap_analysis>
## What's already migrated (all covered)

All 13 tables in db/models/ are covered by the Alembic chain:

Chain order:
  initial_schema_001   → users, hikmah_trees, lessons, lesson_content, user_progress
  userid_to_string     → ALTER user_progress.user_id to VARCHAR(128)
  a12c6d22b9d9         → ALTER user_progress.hikmah_tree_id nullable
  baseline_primers_001 → ADD lessons.baseline_primer_* columns
  personalized_primers_001 → CREATE personalized_primers
  embeddings_001       → CREATE note_embeddings, lesson_chunk_embeddings
  page_quiz_001        → CREATE lesson_page_quiz_questions/choices/attempts
  chat_history_001     → CREATE chat_sessions, chat_messages  ← current HEAD

## What's missing (3 tables)

agents/models/user_memory_models.py defines its own `Base = declarative_base()`
(NOT imported into db/session.py Base), so alembic env.py never sees them.

Tables not in any migration:
  - user_memory_profiles
  - memory_events
  - memory_consolidations

## Column reference from agents/models/user_memory_models.py

### user_memory_profiles
  id                     VARCHAR (UUID string), PK, default uuid4
  user_id                VARCHAR, unique, not null, indexed
  created_at             DateTime (TIMESTAMP with tz)
  updated_at             DateTime (TIMESTAMP with tz)
  display_name           VARCHAR, nullable
  preferred_language     VARCHAR, default 'english'
  timezone               VARCHAR, nullable
  learning_notes         JSON, default []
  interest_notes         JSON, default []
  knowledge_notes        JSON, default []
  behavior_notes         JSON, default []
  preference_notes       JSON, default []
  total_interactions     Integer, default 0
  last_significant_update DateTime
  memory_version         Integer, default 1

### memory_events
  id                       VARCHAR (UUID), PK, default uuid4
  user_memory_profile_id   VARCHAR, FK → user_memory_profiles.id, not null
  event_type               VARCHAR, not null
  event_data               JSON, not null
  trigger_context          JSON, nullable
  notes_added              JSON, default []
  memory_updates           JSON, default []
  processing_reasoning     Text, nullable
  created_at               DateTime
  processed_at             DateTime, nullable
  processing_status        VARCHAR, default 'pending'

### memory_consolidations
  id                       VARCHAR (UUID), PK, default uuid4
  user_memory_profile_id   VARCHAR, FK → user_memory_profiles.id, not null
  consolidation_type       VARCHAR, not null
  notes_before_count       Integer, nullable
  notes_after_count        Integer, nullable
  consolidated_notes       JSON, nullable
  removed_notes            JSON, nullable
  new_summary_notes        JSON, nullable
  consolidation_reasoning  Text, nullable
  created_at               DateTime
</gap_analysis>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration for memory agent tables</name>
  <files>alembic/versions/20260407_create_memory_agent_tables.py</files>
  <action>
Create a new Alembic migration file at alembic/versions/20260407_create_memory_agent_tables.py.

Migration metadata:
  revision = 'memory_agent_001'
  down_revision = 'chat_history_001'   ← appends after current HEAD

upgrade() must create three tables in this order (FK dependency: profiles first):

1. user_memory_profiles
   - id: sa.String(), primary_key=True  (stores UUID string)
   - user_id: sa.String(), nullable=False, unique=True
   - created_at: sa.TIMESTAMP(timezone=True), server_default=sa.func.now()
   - updated_at: sa.TIMESTAMP(timezone=True), server_default=sa.func.now()
   - display_name: sa.String(), nullable=True
   - preferred_language: sa.String(), server_default='english'
   - timezone: sa.String(), nullable=True
   - learning_notes: sa.JSON(), server_default='[]'
   - interest_notes: sa.JSON(), server_default='[]'
   - knowledge_notes: sa.JSON(), server_default='[]'
   - behavior_notes: sa.JSON(), server_default='[]'
   - preference_notes: sa.JSON(), server_default='[]'
   - total_interactions: sa.Integer(), server_default='0'
   - last_significant_update: sa.TIMESTAMP(timezone=True), server_default=sa.func.now()
   - memory_version: sa.Integer(), server_default='1'
   Add index: 'idx_user_memory_profiles_user_id' on ['user_id']

2. memory_events
   - id: sa.String(), primary_key=True
   - user_memory_profile_id: sa.String(), nullable=False, FK → user_memory_profiles.id CASCADE DELETE
   - event_type: sa.String(), nullable=False
   - event_data: sa.JSON(), nullable=False
   - trigger_context: sa.JSON(), nullable=True
   - notes_added: sa.JSON(), server_default='[]'
   - memory_updates: sa.JSON(), server_default='[]'
   - processing_reasoning: sa.Text(), nullable=True
   - created_at: sa.TIMESTAMP(timezone=True), server_default=sa.func.now()
   - processed_at: sa.TIMESTAMP(timezone=True), nullable=True
   - processing_status: sa.String(), server_default='pending'
   Add index: 'idx_memory_events_profile_id' on ['user_memory_profile_id']
   Add index: 'idx_memory_events_status' on ['processing_status']

3. memory_consolidations
   - id: sa.String(), primary_key=True
   - user_memory_profile_id: sa.String(), nullable=False, FK → user_memory_profiles.id CASCADE DELETE
   - consolidation_type: sa.String(), nullable=False
   - notes_before_count: sa.Integer(), nullable=True
   - notes_after_count: sa.Integer(), nullable=True
   - consolidated_notes: sa.JSON(), nullable=True
   - removed_notes: sa.JSON(), nullable=True
   - new_summary_notes: sa.JSON(), nullable=True
   - consolidation_reasoning: sa.Text(), nullable=True
   - created_at: sa.TIMESTAMP(timezone=True), server_default=sa.func.now()
   Add index: 'idx_memory_consolidations_profile_id' on ['user_memory_profile_id']

downgrade() must drop tables in reverse FK order:
  1. memory_consolidations
  2. memory_events
  3. user_memory_profiles

Do NOT use ForeignKeyConstraint list form — use the op.create_table(..., sa.ForeignKeyConstraint([col], [target])) form consistent with the rest of the codebase (see page_quiz_001 and chat_history_001 for reference).

Use sa.text() for server_default string literals (e.g., sa.Column('preferred_language', sa.String(), server_default=sa.text("'english'"))). For JSON columns use server_default=sa.text("'[]'::json") since this is PostgreSQL.
  </action>
  <verify>
    <automated>cd /Users/shawn.n/Desktop/Deen/deen-backend && python -c "
import ast, sys
with open('alembic/versions/20260407_create_memory_agent_tables.py') as f:
    src = f.read()
assert 'memory_agent_001' in src, 'revision ID missing'
assert \"down_revision = 'chat_history_001'\" in src, 'down_revision wrong'
assert 'user_memory_profiles' in src, 'profiles table missing'
assert 'memory_events' in src, 'events table missing'
assert 'memory_consolidations' in src, 'consolidations table missing'
ast.parse(src)
print('OK: migration file is valid Python with correct revision chain')
"
    </automated>
  </verify>
  <done>Migration file exists, parses cleanly, revision='memory_agent_001', down_revision='chat_history_001', and creates all three tables.</done>
</task>

<task type="auto">
  <name>Task 2: Verify alembic history and dry-run upgrade</name>
  <files></files>
  <action>
Verify the complete migration chain is intact and the new migration is recognized.

Run the following checks in order:

1. Confirm alembic can read the chain without errors:
   `alembic history --verbose`
   Expected: 8 revisions listed ending with memory_agent_001 → chat_history_001 → ...

2. Dry-run the upgrade SQL to confirm it generates valid DDL (requires DATABASE_URL to be set in env):
   `alembic upgrade memory_agent_001 --sql`
   If DATABASE_URL is not set, skip this step and note it.

3. If DATABASE_URL is available (Supabase), apply the migration for real:
   `alembic upgrade head`
   Expected: "Running upgrade chat_history_001 -> memory_agent_001" in output with no errors.

If DATABASE_URL is not available in the current shell, document this clearly in the summary so the human can run `alembic upgrade head` when connected to Supabase.
  </action>
  <verify>
    <automated>cd /Users/shawn.n/Desktop/Deen/deen-backend && source venv/bin/activate 2>/dev/null; python -m alembic history 2>&1 | grep -E "memory_agent_001|chat_history_001" | head -5</automated>
  </verify>
  <done>alembic history shows memory_agent_001 as the head revision chained from chat_history_001. If DATABASE_URL is set, `alembic upgrade head` completes without errors.</done>
</task>

</tasks>

<verification>
After both tasks:
- alembic/versions/20260407_create_memory_agent_tables.py exists and is valid Python
- `alembic history` shows 9 revisions with memory_agent_001 as HEAD
- All three tables (user_memory_profiles, memory_events, memory_consolidations) are represented in the migration
- Column definitions match agents/models/user_memory_models.py fields
- FK cascade deletes are correctly ordered (profiles → events/consolidations)
- If Supabase DATABASE_URL is configured: `alembic upgrade head` succeeds
</verification>

<success_criteria>
Running `alembic upgrade head` on a fresh Supabase DB provisions all 16 tables:
- The original 13 from db/models/ (already covered by existing migrations)
- Plus user_memory_profiles, memory_events, memory_consolidations (new migration)

The MemoryProfileRepository, MemoryEventRepository, and MemoryConsolidationRepository can all execute queries against the live Supabase tables without schema errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260407-w1l-migrate-hikmah-tree-and-memory-agent-tab/260407-w1l-SUMMARY.md` following the summary template.
</output>
